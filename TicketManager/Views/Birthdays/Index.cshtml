@model IEnumerable<TicketDataModel.Translator>
@{
    ViewBag.Title = "Дни рождения";
}
<div class="container">
    <div class="row">
        <h2>Дни рождения на ближайший месяц</h2>

        @if (Model.Count() == 0)
        {
            <p>Ничего не найдено</p>
        }
        else
        {
            <table class="table table-condensed">
                <thead>
                    <tr>
                        <th>Имя</th>
                        <th>Дата рождения</th>
                        <th>Должность</th>
                        <th>Подразделение</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var t in Model)
                    {
                        <tr>
                            <td>@t.Name</td>
                            <td>@(t.Dt_Rod.Value.ToString("dd.MM.yyyy"))</td>
                            <td>@(t.Title != null ? t.Title.Name : "хз")</td>
                            <td>@(t.Office != null ? t.Office.Name : "хз")</td>
                        </tr>
                    }
                </tbody>
            </table>
    
            <h4>Всего: @(Model.Count().ToString())</h4><hr />
            <p class="well"><strong>Внимание!</strong> Если чьего-то дня рождения здесь нет, это значит, что этот кто-то или его день рождения не внесен в базу данных сотрудников. </p>
        }

    </div>
</div>
